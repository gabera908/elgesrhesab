# نظام الحسابات الاحترافي — مؤسسة الجسر المصري للإعلام والتنمية

> وثيقة تنفيذية لنظام محاسبة مؤسسي بالقيد المزدوج، يعمل على Docker، بواجهة عربية (RTL) وتصميم مستوحى من هوية Claude.

## 1. الهدف والنطاق (v1)

نواة محاسبية صلبة + التقارير المالية الرئيسية + مراكز التكلفة.

- **اللغة:** عربية (RTL) — **العملة:** الجنيه المصري (EGP) فقط.
- **النشر:** Docker Compose متعدد الحاويات مع TLS.

## 2. المعمارية التقنية

| الطبقة | التقنية |
|---|---|
| الواجهة | React 18 + TypeScript + Vite + Tailwind CSS (RTL) |
| الخادم | FastAPI + SQLAlchemy 2.0 + Alembic (ترحيلات) |
| قاعدة البيانات | PostgreSQL 16 |
| الوسيط | Redis (جلسات / تخزين مؤقت / Rate limiting) |
| البروكسي | Nginx (TLS 1.3 + HSTS + Security Headers) |
| النشر | Docker Compose (شبكة معزولة + أسرار + volumes) |

الحاويات: `db` · `backend` · `frontend` · `nginx` · `redis` — مع healthchecks.

## 3. تصميم قاعدة البيانات

الجداول الرئيسية:

- `companies` — الاسم، الشعار، السجل الضريبي، بداية السنة المالية.
- `accounts` — `code`, `parent_id`, `level (1..4)`, `type` (أصول/خصوم/حقوق ملكية/إيرادات/مصروفات).
- `journals` — نوع: عام/نقدي/بنكي/مبيعات/مشتريات/افتتاحي + الحساب الافتراضي.
- `fiscal_years` / `periods` — السنة المالية والفترات المقفلة.
- `journal_entries` — رقم تسلسلي، المرجع، التاريخ، الحالة (مسودة/مرحَّلة/ملغاة).
- `move_lines` — `account_id`, `debit`, `credit`, `cost_center_id`, `partner_id`.
- `partners` · `cost_centers` · `users` · `roles` · `permissions` · `audit_logs` · `attachments` · `settings`.

### القيود الصارمة

```sql
CHECK (debit >= 0 AND credit >= 0)
CHECK (debit = 0 OR credit = 0)          -- لا يجتمع مدين ودائن في نفس السطر
```

- الترحيل ضمن معاملة واحدة ترفض `Σdebit ≠ Σcredit`.
- قفل الفترات المالية المغلقة.
- لا حذف للقيود المرحَّلة (عكس بدل الحذف).

## 4. الصفحات والموديولات

### 4.1 شجرة الحسابات (المستوى 1→4)
شجرة قابلة للطي/التوسع، أكواد هرمية تلقائية (`1` / `1.1` / `1.1.1`)، تعطيل بدل الحذف، نوع الحساب يحدد موضعه في الميزانية.

### 4.2 القيود اليومية (صفحة الإدخال اليومي)
إدخال متعدد الأسطر، تحقق لحظي من توازن مدين/دائن، قوالب القيود، مرفقات، سير عمل: مسودة → مراجعة → ترحيل.

### 4.3 صفحات المخرجات
- **ميزان المراجعة** — رصيد افتتاحي + حركة + رصيد ختامي + اختبار التوازن التلقائي.
- **اليومية الأمريكية** — قيود اليومية بأعمدة مدين/دائن مع المجاميع.
- **الأستاذ العام** — كشف حركة كل حساب مع الرصيد الجاري.

### 4.4 التقارير
1. **الميزانية العمومية** — أصول/خصوم/حقوق ملكية + اختبار `الأصول = الخصوم + الحقوق`.
2. **قائمة الدخل**.
3. **التدفقات النقدية**.
4. **ميزان المراجعة حسب مركز التكلفة**.
5. كشف حساب الشريك + تقرير شجرة الحسابات + دفتر اليومية.
- تصدير **PDF/Excel/CSV** + صلاحيات لكل تقرير.

### 4.5 تسجيل/دخول آمن
Argon2id + JWT مدوَّر (httpOnly Secure cookie) + **2FA (TOTP)** + قفل الحساب + Rate limiting.

### 4.6 الإعدادات
اسم/شعار المؤسسة · النسخ الاحتياطي المشفَّر (تلقائي/يدوي + استعادة) · التصفير الآمن (تأكيد مزدوج + أرشفة) · الرصيد الافتتاحي للبنوك والصناديق (قيد افتتاحي آلي) · المستخدمون والأدوار · السنوات المالية.

## 5. الأمان (إغلاق الثغرات)

- **الشبكة:** HTTPS فقط + HSTS، شبكة Docker معزولة، لا تعريض منافذ DB.
- **المصادقة:** Argon2id، JWT قصير العمر + Refresh مدوَّر، SameSite، 2FA.
- **التفويض:** RBAC (مدير/محاسب/مراجع/مشاهد) + صلاحيات على مستوى التقرير.
- **الإدخال:** Pydantic/Zod صارم، ORM فقط (لا SQL خام)، تنظيف المرفقات.
- **الحماية:** CSRF tokens، CSP، X-Frame-Options، Rate limiting، XSS escaping.
- **البيانات:** تشفير النسخ الاحتياطية، سجل تدقيق غير قابل للحذف، أسرار في Docker secrets.

## 6. نظام التصميم (هوية Claude)

- **الألوان:** خلفية `#FAF9F5` · حبر `#191919` · مميز كورال `#D97757` · حدود `#E5E3DA`.
- **الخطوط:** Cairo / Tajawal + Noto Sans Arabic، مسافات 8px.
- **التنسيق:** RTL كامل، جداول أنيقة، حالات فارغة، skeletons.
- **الوصولية:** WCAG 2.2 AA، تنقل بلوحة المفاتيح، ARIA.

## 7. خارطة الطريق

| المرحلة | المخرجات |
|---|---|
| 0 | هيكلة المستودع، Docker، CI |
| 1 | الـschema والمصادقة وRBAC وسجل التدقيق ونظام التصميم RTL |
| 2 | شجرة الحسابات، الدفاتر، الرصيد الافتتاحي |
| 3 | القيود اليومية + سير الترحيل + الفترات |
| 4 | الأستاذ العام، ميزان المراجعة، اليومية الأمريكية |
| 5 | الميزانية العمومية، الدخل، التدفقات النقدية |
| 6 | الإعدادات، النسخ الاحتياطي/الاستعادة، التصفير |
| 7 | ترصين الأمان + 2FA + اختبار اختراق داخلي |
| 8 | الاختبارات والتسليم |

## 8. الاختبار

وحدة + تكامل + **اختبارات الثوابت المحاسبية** (توازن القيد، توازن الميزانية) + e2e (Playwright) + CI يمنع الدمج عند الفشل.

## 9. التشغيل

```bash
cp .env.example .env
docker compose up -d        # تشغيل كل الحاويات
docker compose logs -f      # المتابعة
```

- النسخ الاحتياطي: `scripts/backup.sh`
- الاستعادة: `scripts/restore.sh`
